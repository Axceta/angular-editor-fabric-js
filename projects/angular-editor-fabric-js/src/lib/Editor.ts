import { ElementRef } from '@angular/core';
import { CanvasManager } from 'dist/angular-editor-fabric-js/public-api';
import { Canvas, IDataURLOptions } from 'fabric/fabric-impl';

export class Editor {

    /*
    private canvas: Canvas;

    constructor(element: ElementRef) {
        this.canvas = CanvasManager.createCanvas(element.nativeElement, {
        hoverCursor: 'pointer',
        selection: true,
        selectionBorderColor: 'blue'
        });
    }

    public on(event: any) {
      this.canvas.on(event);
    }

    public setHeight(height: number) {
      this.canvas.setHeight(height);
    }

    public setWidth(width: number) {
      this.canvas.setWidth(width);
    }

    public add(element: fabric.Object) {
      this.canvas.add(element);
    }

    public discardActiveObject() {
      this.canvas.discardActiveObject().renderAll();
    }

    public setActiveObject(object: fabric.Object) {
      this.canvas.setActiveObject(object);
    }

    public renderAll() {
      this.canvas.renderAll();
    }

    public setBackgroundColor(color: fabric.Pattern, callback: any) {
      this.canvas.setBackgroundColor(color, callback);
    }

    public getActiveObject(): fabric.Object {
      return this.canvas.getActiveObject();
    }

    public getActiveObjects(): fabric.Object[] {
      return this.canvas.getActiveObjects();
    }

    public getObjects(): fabric.Object[] {
      return this.canvas.getObjects();
    }

    public remove(object: fabric.Object) {
      this.canvas.remove(object);
    }

    public sendToBack(object: fabric.Object) {
      this.canvas.sendToBack(object);
    }

    public toDataURL(options: IDataURLOptions): string {
      return this.canvas.toDataURL(options);
    }

    public toSVG(): string {
      return this.canvas.toSVG();
    }
    */
}
